extends layout
block head
  title #{message}

block content
  div(class="container-full")
    div(class="bg-white shadow-border p-1")
      h1.title= message
      if error
        h2.subtitle= error.status
        div(class="overflow-container")
          pre.font-14 #{error.stack}